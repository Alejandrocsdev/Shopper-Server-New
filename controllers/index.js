const userController = require('./user')
const authController = require('./auth')
const verificationController = require('./verification')

module.exports = { authController, userController, verificationController }